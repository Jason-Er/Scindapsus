CREATE TABLE line (
    ordinal TEXT NULL ,
    audiourl TEXT NULL ,
    scene_id TEXT NOT NULL ,
    id TEXT NOT NULL  PRIMARY KEY ,
    text TEXT NOT NULL ,
    FOREIGN KEY (scene_id) REFERENCES scene (id)
);