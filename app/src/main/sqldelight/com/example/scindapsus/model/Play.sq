CREATE TABLE play (
	id INTEGER NOT NULL  PRIMARY KEY ,
	name TEXT NOT NULL,
	extract TEXT NULL,
	still_url TEXT NULL
);

select_by_id:
SELECT *
FROM play
WHERE play.id = ?;

insertOnePlay:
INSERT INTO play(id, name, extract, still_url)
VALUES (?, ?, ?, ?);

updateOnePlay:
UPDATE play
SET name = ?, extract = ?, still_url = ?
WHERE id = ?;